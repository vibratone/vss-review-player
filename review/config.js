window.VSS_REVIEW_PLAYER_CONFIG = {
  // Make webhook that returns the review-session config and signed audio URLs.
  configWebhookUrl: "",

  // Make webhook that receives the submitted timestamped comments.
  submitWebhookUrl: "",

  // Useful while this project is still being built.
  enableConsolePayloadLogs: true
};
